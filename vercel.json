{
    "builds": [
        {
            "src": "myapp/wsgi.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "myapp/wsgi.py"
        }
    rom django.http import HttpResponse

def home(request):
    return HttpResponse("Hello, Vercel!")
    ]
}